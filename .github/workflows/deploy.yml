name: Build and Deploy ADA Materials

on:
  push:
    branches: [main, master]
    paths:
      - 'content/**'
      - 'docs/**'
      - 'advanced_data_analytics_2025/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main, master]
    paths:
      - 'content/**'
      - 'docs/**'
      - 'advanced_data_analytics_2025/**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  sync-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.SUBMODULE_TOKEN || secrets.GITHUB_TOKEN }}
          
      - name: Update submodules to latest
        run: |
          echo "📚 Updating submodules to latest commits..."
          git submodule update --init --recursive --remote
          echo "✅ Submodules updated to latest"

      - name: Sync content to Jekyll collections
        run: |
          echo "📚 Syncing content/ to Jekyll collections..."
          chmod +x sync-content.sh
          ./sync-content.sh

      - name: Verify submodule files
        run: |
          echo "📚 Verifying submodule files are present..."
          echo "Checking for lecture 3 slides:"
          ls -la docs/advanced_data_analytics_2025/lectures/lecture_3/slides/ || echo "ERROR: Lecture 3 slides not found!"
          echo "Total PDF count in submodule:"
          find docs/advanced_data_analytics_2025 -name "*.pdf" | wc -l
          echo "✅ Submodule verification complete"

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true
          working-directory: ./docs

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4

      - name: Build with Jekyll
        run: |
          cd docs
          echo "Building Jekyll site with baseurl: ${{ steps.pages.outputs.base_path }}"
          bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}" --verbose
          echo "Checking if submodule files are in _site:"
          ls -la _site/advanced_data_analytics_2025/lectures/lecture_3/slides/ || echo "ERROR: Lecture 3 slides not in _site!"
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/_site

  deploy:
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: sync-and-build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4